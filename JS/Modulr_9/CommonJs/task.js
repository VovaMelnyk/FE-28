// const add  = (a, b) => a + b

// const minus = (a, b) => a - b

// module.exports = {
//     add, minus
//     // add: add,
//     // minus: minus
// }

// export const cube = x => x * x * x
// export const pow = (a, b) => Math.pow(a, b)

//  const cube = x => x * x * x
//  const pow = (a, b) => Math.pow(a, b)

//  export {cube, pow}

// export default y => y * y

function sqrt (y) {
    return y * y
}

export default sqrt

// export default function(y) {
//     return y * y
// }